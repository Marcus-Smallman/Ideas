sudo: required
jobs:
  include:
    - stage: build
      install: bash build-scripts/build/install-prerequisites.sh
      script: bash build-scripts/build/build.sh
    - stage: test
      script: echo test
    - stage: deploy
      script: echo deploy